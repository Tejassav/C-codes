
    for(int i = 0 ; i < v.size(); i++){
        cout<<v.at(i)<<" ";
    }
    cout<<endl;

    change(v);

    cout<<"after the function is called.."<<endl;

    for(int i = 0 ; i < v.size(); i++){